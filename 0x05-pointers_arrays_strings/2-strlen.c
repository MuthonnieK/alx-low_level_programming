#include "main.h"
#include <string.h>
/**
 * _strlen - length of a string
 * @s: string to be checked
 * Return: 0 on success
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
